trigger: none

pool:
  vmImage: 'windows-2022'

variables:
  Configuration: 'Release'
  Version: '0.1.1'
  Revision: $[counter(variables['Version'], 1)]

jobs:
- job: Build
  steps:

  - task: NuGetToolInstaller@1
    displayName: "Install NuGet"

  - task: PowerShell@2
    displayName: "Update assembly version"
    inputs:
      filePath: 'build\Update-AssemblyVersion.ps1'
      arguments: '
      -Version $(Version)
      -Revision $(Revision)
      -Tag "$(Build.SourceBranchName)"
      -GitCommit "$(Build.SourceVersion)"'

  - task: NuGetCommand@2
    displayName: "Restore NuGet packages"
    inputs:
      command: 'restore'
      restoreSolution: 'Mandate.CleanArchi.sln'
      feedsToUse: 'config'
      nugetConfigPath: 'NuGet.config'

  - task: VSBuild@1
    displayName: "Build the .NET Standard Mandate Client NuGet packages"
    inputs:
      solution: 'src/Client.Http/Mandate.Client.Http.csproj'
      msbuildArgs: '
      /p:AssemblyVersion=$(Version)
      /p:AssemblyInformationalVersion=$(InformationalVersion)
      /p:OutputPath="$(Build.ArtifactStagingDirectory)\\KPMG.Pulse.Back.Accounting.Mandate.Client.Http\\"'
      configuration: '$(Configuration)'

  - task: PublishBuildArtifacts@1
    displayName: "Publish NuGet Packages artifacts"
    inputs:
      PathtoPublish: '$(Build.ArtifactStagingDirectory)\\'
      ArtifactName: 'NuGet Packages'
      publishLocation: 'Container'

  - task: NuGetCommand@2
    inputs:
      command: 'push'
      packagesToPush: '$(Build.ArtifactStagingDirectory)/**/*.nupkg;!$(Build.ArtifactStagingDirectory)/*/*.symbols.nupkg'
      nuGetFeedType: 'internal'
      publishVstsFeed: '3eae4231-20cf-428b-9e83-aac06ad8acb0/c3b7c547-1a75-4ab9-9f80-e29d4b69d23f'